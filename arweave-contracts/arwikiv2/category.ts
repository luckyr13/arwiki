interface CategoryInfo {
	parent_id: string;
	order: number;
	active: boolean;
}

interface InputAction {}

export function handle(state: Record<string, CategoryInfo>, input: InputAction) {
	const settings_contract = '';
	
}